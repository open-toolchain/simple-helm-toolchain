---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: trigger-template-simple-helm
spec:
  params:
    - name: ibmcloud-api
      description: the ibmcloud api
      default: https://cloud.ibm.com
    - name: repository
      description: The git repo
    - name: branch
      description: the branch for the git repo
      default: "master"
    - name: revision
      description: the git revision/commit for the git repo
      default: ""
    - name: git-token
      description: access token for the git repo
      default: ""
    - name: app-name
      description: application name
    - name: fail-on-lint-errors
      description: force failure of task when docker lint errors are found
      default: "true"
    - name: custom-image-registry-dockerconfigjson
      description: >-
        dockerconfigjson for custom images used in pipeline tasks.
        See https://cloud.ibm.com/docs/devsecops?topic=devsecops-troubleshoot-devsecops#troubleshoot-pipe-abort-early
      default: "eyJhdXRocyI6e319" # ie. {"auths":{}} base64 encoded
    - name: build-script
      description: The command(s) to run the build in run-build step. It will override the default commands
      default: ""
    - name: registry-region
      description: The IBM Cloud region for image registry
    - name: registry-namespace
      description: container registry namespace
    - name: image-name
      description: image name
    - name: path-to-context
      description: the path to the context that is used for the build (`.` meaning current directory)
      default: "."
    - name: path-to-dockerfile
      description: the path to the Dockerfile that is used for the build (`.` meaning current directory)
      default: "."
    - name: dockerfile
      description: The name of the Dockerfile to use for building the image
      default: "Dockerfile"
    - name: fail-on-scanned-issues
      description: force failure of task when vulnerability advisor scan issues are found
      default: "false"
    - name: helm-chart-path
      description: |
        path to the folder containing the helm chart content (at least Chart.yaml).
        If not specified, it will default to first folder in /chart
      default: ""
    - name: cluster-name
      description: the name of the cluster to target
    - name: dev-region
      description: The region that hosts the cluster.
    - name: dev-resource-group
      description: The resource group that the cluster is attached to.
    - name: dev-cluster-namespace
      description: namespace to perform the deployment on the cluster
    - name: helm-upgrade-extra-args
      description: complementary argument for the helm upgrade command. Can be used to specify an additional values file
      default: ""
    - name: pipeline-debug
      description: Pipeline debug mode. Value can be 0 or 1.    
      default: "0"
    - name: commons-hosted-region
      description: the url to retrieve the commons script repository content
      default: "https://raw.githubusercontent.com/open-toolchain/commons/master"
    - name: toolchain-build-image
      description: Image to use for `build` task
      default: "icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.79"
    - name: tester-tests-image
      description: Image to use for `unit-test` task
      default: "icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.79"

  resourcetemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: simple-helm-$(uid)-pvc
      spec:
        resources:
          requests:
            storage: 5Gi
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOnce
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(params.custom-image-registry-dockerconfigjson)
      metadata:
        name: custom-image-registry-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: simple-helm-$(uid)
      spec:
        pipelineRef:
          name: simple-helm-pipeline
        params:
          - name: ibmcloud-api
            value: $(params.ibmcloud-api)
          - name: repository
            value: $(params.repository)
          - name: branch
            value: $(params.branch)
          - name: revision
            value: $(params.revision)
          - name: git-token
            value: $(params.git-token)
          - name: app-name
            value: $(params.app-name)
          - name: fail-on-lint-errors
            value: $(params.fail-on-lint-errors)
          - name: registry-region
            value: $(params.registry-region)
          - name: registry-namespace
            value: $(params.registry-namespace)
          - name: image-name
            value: $(params.image-name)
          - name: path-to-context
            value: $(params.path-to-context)
          - name: path-to-dockerfile
            value: $(params.path-to-dockerfile)
          - name: dockerfile
            value: $(params.dockerfile)
          - name: fail-on-scanned-issues
            value: $(params.fail-on-scanned-issues)
          - name: helm-chart-path
            value: $(params.helm-chart-path)
          - name: dev-region
            value: $(params.dev-region)
          - name: dev-resource-group
            value: $(params.dev-resource-group)
          - name: cluster-name
            value: $(params.cluster-name)
          - name: dev-cluster-namespace
            value: $(params.dev-cluster-namespace)
          - name: helm-upgrade-extra-args
            value: $(params.helm-upgrade-extra-args)
          - name: pipeline-debug
            value: $(params.pipeline-debug)
          - name: commons-hosted-region
            value: $(params.commons-hosted-region)
          - name: toolchain-build-image
            value: $(params.toolchain-build-image)
          - name: build-script
            value: $(params.build-script)
          - name: tester-tests-image
            value: $(params.tester-tests-image)
        workspaces:
          - name: pipeline-ws
            persistentVolumeClaim:
              claimName: simple-helm-$(uid)-pvc
        podTemplate:
          imagePullSecrets:
            - name: custom-image-registry-pull-secret
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: trigger-binding-manual
spec:
  params:
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: manual-run
  annotations:
    tekton.dev/displayName: "manual run listener"
spec:
  triggers:
    - binding:
        name: trigger-binding-manual
      template:
        name: trigger-template-simple-helm
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: github-ent-commit
spec:
  params:
    - name: branch
      value: $(event.ref)
    - name: commit-id
      value: $(event.after)
    - name: commit-timestamp
      value: $(event.repository.pushed_at)
    - name: repository
      value: $(event.repository.html_url)
    - name: scm-type
      value: "github-ent"
---        
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: github-ent-commit
  annotations:
    tekton.dev/displayName: "github enterprise commit push event listener"
spec:
  triggers:
    - bindings: 
        - name: github-ent-commit   
      template:
        name: trigger-template-simple-helm
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: github-commit
spec:
  params:
    - name: branch
      value: $(event.ref)
    - name: commit-id
      value: $(event.after)
    - name: commit-timestamp
      value: $(event.repository.updated_at)
    - name: repository
      value: $(event.repository.html_url)
    - name: scm-type
      value: "github"
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: github-commit
  annotations:
    tekton.dev/displayName: "github commit push event listener"
spec:
  triggers:
    - bindings:  
        - name: github-commit   
      template:
        name: trigger-template-simple-helm
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: grit-or-gitlab-commit
spec:
  params:
    - name: branch
      value: $(event.ref)
    - name: commit-id
      value: $(event.checkout_sha)
    - name: commit-timestamp
      value: $(event.commits[0].timestamp)
    - name: repository
      value: $(event.project.http_url)
    - name: scm-type
      value: "gitlab"
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: grit-or-gitlab-commit
  annotations:
    tekton.dev/displayName: "GRIT/gitlab commit push event listener"
spec:
  triggers:
    - bindings:   
        - name: grit-or-gitlab-commit
      template:
        name: trigger-template-simple-helm
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: bitbucket-commit
spec:
  params:
    - name: repository
      value: "$(event.repository.links.html.href)"
    - name: revision
      value: "$(event.push.changes[0].new.target.hash)"
    - name: branch
      value: "$(event.push.changes[0].new.name)"
    - name: commit-timestamp
      value: "$(event.pull_request.head.repo.pushed_at)"
    - name: commit-id
      value: "$(event.pull_request.head.sha)"
    - name: scm-type
      value: "bitbucket"
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: bitbucket-commit
  annotations:
    tekton.dev/displayName: "bitbucket commit push event listener"
spec:
  triggers:
    - binding:
        name: bitbucket-commit
      template:
        name: trigger-template-simple-helm